#include "stdio.h"

int main()
{
	
	int *a[2];
	int b[2];
	int c;

	// This shouldnt be allowed
	//a = &b;

	// TODO: Fix this
	*(b+0) = 1;
	writeInteger(b[0]);
	a[0][0] = 2;
	writeInteger(a[0][0]);
	writeInteger(b[0]);
	return 0;
}
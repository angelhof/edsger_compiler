#include "stdio.h"
int main()
{
	int a; 
	int b;
	int c;
	double d;
	double e;

	e= 6.0;
	d = 0.0;

	c = 1;
	writeInteger(c);
	b = 0;
	writeInteger(b);
	a = b;
	writeInteger(a);
	a = b = c;
	writeInteger(a);
	writeInteger(b);

	d = --e;
	d ++;
	writeInteger(++ a);

	a = --b;
	writeInteger(a);	
	


	return a--;
}
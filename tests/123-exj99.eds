#include "stdio.h"

void main ()
{
   int a, x, y, z, r, s, i, n;

   void proc1 (int x, byref int y)
   {
      int q;

      y = a + x * z;
   }

   a=1; x=2; y=3; z=4; r=5; s=6; i=0; n=8;
   
   if (a+x>y)
      for (; i <= n; ) {
         s = s + i;
         i = i + 1;
      }
   else if (x > y || x < z) z = 3;
   else proc1(x+y,r);

   writeInteger(a);
   writeInteger(x);
   writeInteger(y);
   writeInteger(z);
   writeInteger(r);
   writeInteger(s);
   writeInteger(i);
   writeInteger(n);
   
   writeString("\n---\n");
   writeString("12335608\n");
}

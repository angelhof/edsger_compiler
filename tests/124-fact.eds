#include "stdio.h"

void main ()
{
   int fact (int n)
   {
      if (n < 0)
         return -1;
      else if (n == 0)
         return 1;
      else
         return fact(n-1) * n;
   }

   writeInteger(fact(7));
   writeString("\n");
}

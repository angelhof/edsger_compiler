
1) FIX the multihack reduce /reduce error

2) FIX the arrays operators valid only for pointers in the same matrix Dynamically

3) Change all exits in parser with exit funcs

4) Change line errors
    For example it doesnt count the comment lines

5) Todo cast bonus (Apo poia se poia kai pws )

6) Make the included libraries return asts also

7) Fix warning when you have declared but not defined a function, it gives error that you redefine it

8) Check on pantelaios files, fix error make better error message for types

9) Comma shouldnt check for type equality, also it returns the second

10) L - Value check on left, right and whole assignment and on ufo

11) FIX --- int main without return passes

12) FIX --- Check why for true doesnt work